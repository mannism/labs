/**
 * Barrel export for the Voice Particle Instrument experiment.
 * VoiceParticleCanvas is the primary export — loaded via next/dynamic
 * with ssr: false from ExperimentDetail.
 */
export { VoiceParticleCanvas } from "./VoiceParticleCanvas";
